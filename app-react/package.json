{
  "name": "app-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "react-scripts": "3.2.0",
    "kotlin-mpp-npm-demo-mpp": "0.0.1"
  },
  "scripts": {
    "start": "../gradlew build -p ../ && yarn --cwd .. && react-scripts start",
    "build": "../gradlew build -p ../ && yarn --cwd .. && react-scripts build",
    "test": "../gradlew build -p ../ && yarn --cwd .. && react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
